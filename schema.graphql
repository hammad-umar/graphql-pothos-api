type Mutation {
  createUser(email: String!, name: String!): User!
}

type Query {
  hello: String!
  user(id: Int!): User!
  users: [User!]!
}

"""Represents User"""
type User {
  email: String!
  id: Int!
  name: String!
  verified: Boolean!
}